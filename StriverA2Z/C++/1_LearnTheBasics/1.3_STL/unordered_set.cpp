// lower_bound and upper_bound function does not works.
// rest all functions are same as above.
// it does not store any particular order it has a better complexity than set in most cases.
// except some when collision happens.

// Mostly O(1) is the time complexity.

// no sorted order
// but unique

#include<bits/stdc++.h>

using namespace std;

int main() {

    return 0;
}